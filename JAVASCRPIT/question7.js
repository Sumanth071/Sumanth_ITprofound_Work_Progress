let x = 10;
let y = 20;
let z = 30;

// Using subtraction and negation to calculate sum without '+'
let sum = x - (-y) - (-z);
console.log(sum);

// Explanation:
// x - (-y) is equivalent to x + y
// (x + y) - (-z) is equivalent to (x + y) + z
// Therefore, sum = x + y + z
// Output the result